import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      light: '#0d6efd',
      main: '#0d6efd',
      dark: '#0d6efd',
    },
    secondary: {
      light: '#0d6efd',
      main: '#0d6efd',
      dark: '#0d6efd',
    },
  },
});
